# Segredos e estado de runtime das rotinas. Nada disto entra no git.
# (a raiz do repo ja ignora .env*, *.pem e .vercel; isto aqui e reforco local)
.friday-secret
*.key
*.pem
*.secret

# estado de runtime das rotinas (cursor da caixa, flag de maquina ativa)
.friday-last-seen-*.json
active-machine.json
.friday-machine.id

# guard de maquina ativa: config local com o endereco do servidor de coordenacao.
# O .example fica versionado (e so um modelo); o real, NUNCA.
.friday-vds.json
machine-guard/.friday-vds.json
GUARD-ERRO.log
machine-guard/GUARD-ERRO.log
machine-guard/presence/
presence/
